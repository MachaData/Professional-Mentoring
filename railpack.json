{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "php artisan migrate --force --seed || true; php artisan storage:link || true; php artisan serve --host 0.0.0.0 --port ${PORT}"
  }
}
