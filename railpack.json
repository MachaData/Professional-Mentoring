{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "php artisan migrate --force || true; php artisan db:seed --class=RolePermissionSeeder --force || true; php artisan storage:link || true; php artisan serve --host 0.0.0.0 --port ${PORT}"
  }
}
